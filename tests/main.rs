use macros::remote_function;
use std::process::Command;

#[test]
fn test_remote_function() {

    // 1. spin up the server via cargo watch -x 'run --bin minimodal-server'
    // 2. make request to 

}